**Конспект: HTTP vs HTTPS**  

**1. Определения**  
- **HTTP (HyperText Transfer Protocol)**: Протокол передачи данных, используемый для передачи информации между браузером и веб-сервером.  
- **HTTPS (HyperText Transfer Protocol Secure)**: Защищённая версия HTTP с использованием шифрования для защиты данных.  

**2. Основные отличия**  

| **Характеристика**         | **HTTP**                          | **HTTPS**                           |  
|-----------------------------|-----------------------------------|-------------------------------------|  
| **Безопасность**           | Данные передаются в открытом виде, легко перехватываются. | Данные шифруются, защищены от перехвата (TLS/SSL). |  
| **Шифрование**             | Отсутствует.                     | Использует SSL/TLS для шифрования. |  
| **Скорость**               | Работает быстрее из-за отсутствия шифрования. | Чуть медленнее из-за дополнительных операций шифрования. |  
| **Сертификаты**            | Не требует сертификатов.          | Требует SSL/TLS-сертификатов.       |  
| **Пример URL**             | `http://example.com`             | `https://example.com`              |  

**3. Как работает HTTPS?**  
1. Клиент (браузер) инициирует соединение с сервером.  
2. Сервер предоставляет SSL/TLS-сертификат для проверки своей подлинности.  
3. Устанавливается зашифрованное соединение (TLS Handshake).  
4. Данные передаются в зашифрованном виде.  

**4. Преимущества HTTPS**  
- **Безопасность данных**: Защищает от перехвата и атак "человек посередине" (Man-in-the-Middle).  
- **Доверие пользователей**: В браузерах отображается замок (🔒), указывающий на безопасное соединение.  
- **SEO**: Поисковые системы (например, Google) отдают предпочтение сайтам с HTTPS.  
- **Соответствие стандартам**: Требуется для обработки платежей и передачи конфиденциальных данных.  

**5. Сертификаты SSL/TLS**  
- **Виды сертификатов**:  
  - **DV (Domain Validation)**: Проверка только домена.  
  - **OV (Organization Validation)**: Проверка организации.  
  - **EV (Extended Validation)**: Максимальная степень проверки, отображается зелёная строка в браузерах.  
- Выдаются центрами сертификации (CA), например, Let's Encrypt, DigiCert.  

**6. Уязвимости HTTP**  
- Легко перехватывается злоумышленниками (сниффинг).  
- Уязвимость к подмене данных и фишинговым атакам.  

**7. Когда использовать HTTPS**  
- Для всех сайтов, особенно тех, которые обрабатывают персональные данные, платежи или чувствительную информацию.  

**Вывод**: HTTPS значительно превосходит HTTP с точки зрения безопасности и доверия пользователей. Сегодня использование HTTPS стало стандартом для всех профессиональных веб-ресурсов.  
